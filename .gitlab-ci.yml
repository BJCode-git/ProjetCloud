docker-build:
  # Use the official docker image.
  image: docker:cli
  stage: build
  services:
    - docker:dind

  before_script:
    - echo "dckr_pat_6ozrng5x5m16k68os4kQkO17NqI" | docker login -u "jbunistra" --password-stdin 
  script:
    - docker build . --tag cloud-front-end
    - docker push 
    - docker.io/jbunistra/cloud-front-end:latest
  # Run this job in a branch where a Dockerfile exists
  rules:
    - if: $CI_COMMIT_BRANCH
      exists:
        - Dockerfile
