docker-build:
  image: docker:19.03
  stage: build
  services:
    - docker:19.03-dind

  before_script:
    - echo "dckr_pat_6ozrng5x5m16k68os4kQkO17NqI" > docker login -u "jbunistra" --password-stdin 
  script:
    - sudo docker build . --tag cloud-front-end
    - sudo docker push docker.io/jbunistra/cloud-front-end:latest
  # Run this job in a branch where a Dockerfile exists
  rules:
    - if: $CI_COMMIT_BRANCH
      exists:
        - Dockerfile
